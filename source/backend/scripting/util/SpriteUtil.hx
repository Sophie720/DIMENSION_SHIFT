package backend.scripting.util;

class SpriteUtil
{
    public function center(spr:FlxSprite, pos:String = 'xy')
    {
        switch (pos.toLowerCase().trim())
        {
            case 'x':
                spr
        }
    }
}